app = "ai-tech-radar"
primary_region = "arn"

[build]
  dockerfile = "Dockerfile"


[env]
  NODE_ENV = "production"
  TZ = "Europe/Copenhagen"
  PORT = "3000"

[http_service]
  internal_port = 3000
  force_https = true
  auto_stop_machines = "stop"
  # F391 24/9 2026 (ejerens GO): 3 svar på 7 døgn — kold
  auto_start_machines = true
  min_machines_running = 0
  # F391 24/9 2026 (ejerens GO): 3 svar på 7 døgn — kold
[[http_service.checks]]
  grace_period = "30s"
  interval = "60s"
  method = "GET"
  path = "/health"
  timeout = "5s"

[[vm]]
  size = "shared-cpu-1x"
  memory = "512mb"

[[mounts]]
  source = "radar_data"
  destination = "/data"
